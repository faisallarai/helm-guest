apiVersion: v1
kind: Service
metadata:
  name: { { template "helm-guest.fullname" . } }
  labels:
    app: { { template "helm-guest.name" . } }
    chart: { { template "helm-guest.chart" . } }
    release: { { .Release.Name } }
    heritage: { { .Release.Service } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: { { template "helm-guest.name" . } }
    release: { { .Release.Name } }
